# Dependencies needed by the demo, dashboard and GitHub/Streamlit deployment.
# EnergyPlus itself is installed separately from energyplus.net; PyEnergyPlus
# is bundled with that installation and is therefore deliberately not a pip
# dependency.
streamlit>=1.40
pandas>=2.2
numpy>=1.26
plotly>=5.24
requests>=2.31
pydantic>=2.4
